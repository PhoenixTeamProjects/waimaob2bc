{
  "name": "waimaob2bc",
  "private": true,
  "version": "0.1.0",
  "description": "Phoenix personal foreign-trade brand website.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build && node scripts/hostinger-export.mjs",
    "start": "npm --workspace frontend run start",
    "lint": "npm --workspace frontend run lint",
    "typecheck": "npm --workspace frontend run typecheck"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
